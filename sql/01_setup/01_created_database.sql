-- Query 1 (Drops database if same database already exists)
-- Always better to use this Command if starting a new project)
DROP DATABASE IF EXISTS modern_library;


-- Query 2 (Creates database)
CREATE DATABASE modern_library;


-- Query 3 (Uses / switch to database)
USE modern_library;